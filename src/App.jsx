import './App.css'

function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">React template</p>
        <h1>Ship a tested React app in an nginx container.</h1>
        <p className="hero-copy">
          This starter includes Vite, ESLint, Vitest, Docker, nginx, and a
          GitHub Actions pipeline that validates every push.
        </p>
      </section>
    </main>
  )
}

export default App
